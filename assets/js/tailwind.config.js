/** @type {import('tailwindcss').Config} */
tailwind.config = {
    content: ["./.{html}"],
    theme: {
        extend: {
            fontFamily: {
                'rubik' : ["Rubik", "sans-serif"]
            },
            colors: {
                'biruMiaw' : '#86D6F2'
            }
        }
    }
}